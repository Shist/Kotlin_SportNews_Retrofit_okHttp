<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.compose_app">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ComposeUiApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_compose"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_compose"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlin_SportNews_Retrofit_okHttp">
        <activity
            android:name="com.compose_ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Kotlin_SportNews_Retrofit_okHttp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>